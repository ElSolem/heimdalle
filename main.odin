package main

import "core:fmt"
import ex "examples"

main :: proc() {
    // ex.AbraxasGate()
    // ex.Alakazan()
    fmt.println(ex.Abraxas())
    fmt.println(ex.Vivaos())
    fmt.println(ex.Alakazan())
}